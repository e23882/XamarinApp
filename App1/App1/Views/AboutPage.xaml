<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App1.Views.AboutPage"
             xmlns:vm="clr-namespace:App1.ViewModels"
             Title="{Binding Title}">

    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center" Orientation="Horizontal">
            <Button VerticalOptions="Center" Text="A" Command="{Binding ClickCommand}" CommandParameter="A"/>
            <Button VerticalOptions="Center" Text="B" Command="{Binding ClickCommand}" CommandParameter="B"/>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
